[package]
name = "{project_name}"
version = "0.0.1"
edition = "2021"

[dependencies]
balius-sdk = "0.3.4"
serde = { version = "1.0.204", features = ["derive"] }

[lib]
crate-type = ["cdylib"]

[package.metadata.release]
release = false